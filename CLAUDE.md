# ERP-Cafe

Java/Spring Boot backend (`backend/`) + Angular frontend (`frontend/`).

## Agent skills

### Issue tracker

Issues live as GitHub issues on `Baoteiha/ERP-System`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using their default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
